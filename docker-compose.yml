services:
  shark-next:
    container_name: shark-next
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports: 
      - 4003:3000
    environment:
      - PORT=3000
      - NODE_ENV=production
    networks:
      - client-network

networks:
  client-network:
    driver: bridge
